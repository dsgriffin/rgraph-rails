require 'spec_helper'

describe RgraphRails do
  it 'has a version number' do
    expect(RgraphRails::VERSION).not_to be nil
  end

  it 'does something else' do
    expect(true).to eq(true)
  end
end
